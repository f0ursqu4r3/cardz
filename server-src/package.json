{
    "name": "cardz-server",
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "bun run --watch index.ts",
        "start": "bun run index.ts",
        "typecheck": "tsc --noEmit",
        "test": "bun test",
        "test:watch": "bun test --watch"
    },
    "dependencies": {
        "nanoid": "^5.0.9",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "@types/bun": "^1.2.14"
    }
}
